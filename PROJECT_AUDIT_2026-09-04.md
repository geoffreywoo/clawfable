# Clawfable creative learning audit and Astra rollout

Implementation base: `41b5c5d8f59fe4e90063dd421d50033beac39eb5` (current production/main at inspection). The prior local baseline was 1,235 passing tests; this work starts from newer main and preserves its CI, reward-confidence, and anti-slop changes.

## Confirmed findings and repairs

| Finding / reproduction | Repair | Regression evidence |
| --- | --- | --- |
| Positive feedback was included among negative preference instructions. An approval could teach the system to avoid the approved style. | Separate feedback polarity throughout derived memory and reject inferred removal assumptions. | `learning-loop.test.ts`, `learning-evidence.test.ts` |
| Long oldest-first coaching sections could exhaust the prompt budget before the newest correction. | Prioritize complete recent directives before applying the prompt budget, preserving explicit newer-rule precedence. | `generation-astra-prompt.test.ts`, voice-guidance regressions |
| Queue edits, manual-post children, and calibration stored inconsistent or truncated edit context. | Shared complete before/after metadata, transformation lessons, parent-child lineage; retrieve at most two complete relevant approved pairs, excluding superseded/rejected revisions. | `learning-loop.test.ts`, `queue-ownership.test.ts`, `calibration-route.test.ts`, `draft-lineage.test.ts` |
| Early performance checkpoints counted toward final rewards and training baselines. | Require finite measurement timestamps at least 18 hours after posting for final performance learning. Explicit feedback remains immediate; early velocity remains descriptive. | `outcome-rewards.test.ts`, `bandit.test.ts`, `performance-learning.test.ts`, `performance-tracking.test.ts` |
| Timeline omission was interpreted as removal and negative editorial intent. Missing replies and provider trouble could therefore poison preferences. | Official per-ID lookup returns present/not_found/unavailable. Require two explicit not-found observations at least 15 minutes apart. Unavailable observations do not confirm removal; presence resets the count. Confirmed removal has unknown reason. | `twitter-availability.test.ts`, `x-removal-reconciliation.test.ts`, `soul-evolution.test.ts` |
| Skipping the deletion-reason prompt still inferred a negative preference and replaced the verified removal signal with an unverified guess. | Dismiss the prompt without inventing feedback or changing the existing removal evidence. Explicit supplied reasons retain their normal learning behavior. | `queue-ownership.test.ts` |
| Process-local locks did not protect shared array ledgers across Vercel instances. Concurrent read/modify/write updates could discard learning or candidate records. | Redis Lua snapshot/revision compare-and-swap for learning, outcome, generation-outcome, candidate, and run ledgers; stable IDs and idempotent experiment reward projection. Local fallback retains per-key serialization. | `kv-storage-distributed.test.ts`, storage concurrency suites |
| A manual posting request could use tweet state read before acquiring its lock. | Reload fresh state under the lock; recheck ownership, content, parent, type, and lifecycle before an X call. | `twitter-post-route.test.ts` |
| Autopilot's final re-read could hit the request cache after an operator changed the queued draft, even with unchanged queue-version readings around selection. | Bypass the cache and revalidate ownership, X receipt, lifecycle, lineage, and account eligibility; preserve the final queue-version guard. | `autopilot-logging.test.ts` |
| Agent list aggregation could include a recycled-handle account that direct ownership checks denied. | Every listing candidate passes the shared `canAccessAgent` policy used by dashboard and companion access. | `account-access.test.ts` |
| Stripe's reservation was also treated as a completed event. Interrupted processing could acknowledge an unprocessed retry. | Separate short owner lease from durable completion receipt; busy and failed verified events stay retryable. Recover ambiguous lease responses; complete only after processing succeeds. | `stripe-webhook.test.ts`, `kv-storage-distributed.test.ts` |
| Concurrent user updates merged full cached snapshots: an email update could restore paid status after a refund. | Atomically merge each user patch against the current shared record, preserving unrelated concurrent fields. | `kv-storage-distributed.test.ts`, `kv-storage-users.test.ts` |
| Atomic hash decoding reparsed large X IDs through JavaScript numbers; a local regression changed `1777777777777777777` to `1777777777777777700`. | Preserve unsafe-integer identities as exact strings, including SDK-decoded values, while retaining ordinary numeric/boolean/object values. Verified through user updates and username lookup. | `kv-atomic.test.ts`, `kv-storage-distributed.test.ts` |
| Dynamic seed prose lost its qualified evidence when converted into briefs. | Resolve source-document IDs to qualified current story claims; synthesized prose stays inspiration, never evidence. | `generation-creativity.test.ts`, `generation-v2-integration.test.ts` |
| Ideation saw previous drafts inside nested memory sections, encouraging copied premises. Variants lacked distinct editorial jobs. | Strip raw draft prose from ideation while retaining extracted lessons. Astra variants develop a direct judgment, concrete decision, or unexpected consequence from the same permitted evidence. Writers and judges receive approved edit pairs. | `generation-creativity.test.ts` |
| Exploration labels did not implement the configured random selection policy. Geoffrey-specific topical rules also leaked to other profiles. | Astra performs at most one exploration choice per batch, among fully eligible near-ties within 0.03 quality-margin points of the best feasible choice. Store eligible IDs and propensity. Geoffrey's restrictions and allocation apply only to Geoffrey. | `generation-creativity.test.ts`, `generation-v2.test.ts` |
| Provider routing had no Astra capability definition or controlled account rollout. Partial response text could be accepted after truncation. | Separate `publishing_v2_astra` stack, account rollout switch, explicit reasoning compatibility, no sampling parameters, rejection of incomplete outputs, existing bounded deadlines/fallbacks, and requested/actual model provenance. | `ai-routing.test.ts` |
| The first live Astra preview exhausted both 75-second ideation calls. Queueing smaller calls could also exceed the intended overall budget because the old deadline checks only ran between stages. | Astra requests three propositions per brief with at most four concurrent calls. Every generation AI stage is clamped to its run's remaining 240-second budget, isolated across concurrent runs. Preserve wire model identity and failure provenance. | `generation-v2-integration.test.ts`, `ai-routing.test.ts`; live follow-up receipt required |

## Model and creative controls

`ASTRA_CREATIVE_ROLLOUT` is `off` by default (absent/invalid values preserve the current stacks). `geoffrey` opts in only the recognized Geoffrey aliases; `all` is reserved for a successful later promotion. All creative stages, replies, editorial judgment, learning, and voice synthesis resolve the account stack. Classification and source-enrichment utilities retain their existing models. Explicit preview comparison stacks remain independent.

Astra defaults to high reasoning for creative work and learning, medium for judges, and at least 8,192 output/reasoning tokens within existing run deadlines. Inherited `none`/`minimal` settings normalize to `low`; supported efforts run through `max`. No `temperature` is sent. Fallbacks use the same downstream eligibility gates. Traces distinguish requested model, actual model, reasoning, skipped/unavailable providers, incomplete responses, tokens, latency, and estimated cost.

Pricing checked against [OpenAI pricing](https://developers.openai.com/api/docs/pricing) on 2026-09-04: Astra standard uncached input $10/M and output $50/M tokens; over 272k input tokens, input is 2x and output 1.5x. The current `gpt-5.6` alias resolves to `gpt-5.6-sol`, whose documented promotional pricing is $4/M input and $20/M output, with the same long-context multipliers, available at least through 2026-11-21. See [GPT-5.6 Sol](https://developers.openai.com/api/docs/models/gpt-5.6-sol). Estimates retain cache token counts but use uncached input rates; they are not invoice reconciliation. Capability source: [Astra migration guide](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra).

## Derived history repair

The protected `POST /api/internal/agents/:id/generation/quality/refresh` accepts `{"mode":"derived_only"}`. It acquires the existing account lock, rebuilds learning summaries, and replays experiment reward projections from retained verified evidence. It does not read X, refill or alter the queue, publish, or rewrite the SOUL. Insight synthesis can call the configured learning model. Raw signals, tweets, feedback, and outcomes remain preserved.

Summary derivation version `learning-2026-09-04-v3` records mature/immature counts, recovered edit signal IDs, excluded unverified removal counts, and reward math version `mature-spread-action-2026-09-04-v1`. Mature action rewards are recalculated from measured metrics and current mature baselines, replacing legacy cached calculations only in derived views/projections. Cron rebuilds stale derivation versions. Recovered examples reach subsequent generation context.

Rebuilds cannot recover records already lost to historical races or retention limits. Complete edit pairs are recoverable only when intact lineage and trustworthy original/edited text still exist. Performance maturity depends on actual measurement time, not the current age of a formerly early checkpoint.

## Evaluation and release gates

The frozen evaluation uses 40 packets: 30 Geoffrey and 10 explicitly synthetic other-account profiles. Both stacks receive identical frozen briefs and evidence. Account reference examples are held out from prompt anchors, with their original authorship provenance retained. A timeline reference is not proof of human composition. Primary model fallback invalidates a comparison as Astra evidence. The evaluation records eligibility, ideas, factual failures, copied premises, estimated edit burden, latency, cost, and blinded preference. A score must identify its reviewers; model votes are not human votes and edit estimates are not observed edits.

Promotion requires at least 30 decisive comparisons, at least 60% Astra preference, all 40 comparisons completed, and no hard-gate regression. Empty or unqualified content does not satisfy the gate. Frozen evidence is as of capture, not a claim of current news accuracy.

Reproduce from a validated private snapshot using `npm run eval:astra -- --validate --snapshot FILE`, then `npm run eval:astra -- --run --snapshot FILE --remote-origin https://www.clawfable.com --max-cost-usd 100`. Optional `--concurrency 1..4` runs independent packets concurrently; the default is one, and each packet's alternating model arms stay sequential. Supply `CRON_SECRET` through the local environment, never a command argument. Remote execution sends a hashed gzip packet per arm to the authenticated evaluation endpoint; it enforces 2 MiB compressed/8 MiB expanded limits and preview-only production gates. The first response pins the deployment SHA; later requests reject a changed deployment before generation, and all returned identities are checked. It does not export provider credentials. Snapshot capture and all result files stay under the ignored `.gstack/astra-evaluation/` directory. The cost ceiling stops new arms at observed spend; already-running arms can exceed the remaining estimate. Invalid arms stop new scheduling while active calls drain. An uncertain network result is not automatically retried because it may already have incurred provider cost. Ordered, serialized progress receipts preserve returned paid arms; SIGINT/SIGTERM stop scheduling and drain active work. A hard process kill cannot guarantee receipt persistence.

Review only the generated blinded cards, identify the human or independent critic in the votes file, then run `npm run eval:astra -- --score --comparison FILE --votes FILE`. A synthetic fixture is not a human preference observation. Separate model-review estimates from observed operator edit burden during the live pilot.

Release order:

1. Ship shared correctness and gated Astra support with rollout disabled. Verify the exact Vercel deployment, production aliases, and health commit.
2. Allow older in-flight invocations to drain before replaying projections, because an old deployment does not participate in new CAS revisions.
3. Run the derived-only rebuild and inspect the protected account audit. Preserve raw history and the posting cadence.
4. Validate the production OpenAI credential and actual Astra access, complete the frozen comparison, then enable only `geoffrey` and redeploy.
5. Verify actual model traces and queue eligibility. Leave the queue underfilled when quality gates reject content.
6. Consider `all` only after at least seven days and ten mature pilot posts, with no approval/edit burden or mature-performance regression. Insufficient evidence retains the pilot.

## Production observations and limits

Read-only inspection of connected agent 13 (`@geoffwoo`) found one eligible queued draft on the existing GPT control stack and existing audience voice complaints, low generation yield, and insufficient queue depth. The existing learning summary covered 722 posts; the active corpus had 32 of its target 40 anchors. Retrieved voice references carried `timeline_unmatched` provenance. The two explicitly pinned posts were media/quote dependent and excluded from standalone diction anchors. This is a concrete quality bottleneck; a stronger model is not proof of better copy or future virality.

The downloaded Production `OPENAI_API_KEY` is blank because the project stores it as a Vercel sensitive variable. Treating that export as evidence of a missing runtime credential was incorrect. The deployed runtime attempted Astra calls; the initial non-persisting preview returned no drafts because both six-proposition ideation requests hit their 75-second stage limit. This establishes a runtime timeout, not successful Astra output or a model-quality result. A follow-up divides Astra ideation into three-proposition requests with bounded concurrency, preserves the total idea opportunities and deadlines, and records requested model/reasoning on failures. Timeout improvement must be established by a subsequent live receipt. The pilot remains disabled pending the frozen comparison gate.

Distributed concurrency regressions use independent module runtimes and a shared Redis-script test double; they do not certify every production Redis failure mode. CAS covers the affected ledgers and user patch updates, not every remaining read/modify/write object. Atomic user patches preserve disjoint fields but do not order semantically conflicting events from different Stripe event IDs. Legacy Stripe reservation receipts are preserved because their historical completion state cannot safely be inferred; existing invoice reconciliation remains necessary for older missed events. Existing bounded X lookup budgets can delay removal confirmation. No automatic SOUL migration or new analytics dashboard is introduced.

Independent review covered distributed storage, replay, Stripe, manual publishing, model compatibility, and exploration; discovered races and stale-state gaps were fixed and regression tested. Full-suite and deployment receipts are recorded below after verification.

The shared-correctness release passed 144 test files / 1,349 tests, TypeScript, and production build both on PR #119 and merged main. PR #119 merged as `f233fbd9fdd56a7359d3e2a490ee302c353f7adf`. Exact deployment `dpl_2P7sTWJ29GCrMStevNPzjhjPcK6y` became Ready at 2026-09-04 22:11:41 UTC. Both `clawfable.com` and `www.clawfable.com` aliases were confirmed on that deployment; homepages returned 200 and both health endpoints reported the exact merged SHA.

After the older in-flight autopilot invocation released its own lock, the derived-only rebuild completed at 2026-09-04 22:13 UTC. It updated 2,373 experiment records, excluded 15 unverified removal signals, and recorded 706 mature and 17 immature posts within the learning-summary retention window. The subsequent audit found two eligible queued GPT-control drafts and no quarantined drafts. These counts differ from the broader retained-history inspection below because the readers have different limits. No queue refill, SOUL rewrite, or post was initiated by the repair.

The final post-rebuild frozen snapshot captured and validated all 40 distinct packets with hash `ccbc132b32ddc6847b15e47e29a036dd99c84acc4d48396c5a24a05b17e7c375`, three separate unverified-authorship account references, and 16 calibration anchors. The pre-rebuild snapshot is retained as an earlier receipt. Local execution stopped before provider calls because Vercel sensitive credentials are unavailable in its export; a protected remote preview adapter allows evaluation against the same frozen inputs without exporting the credential.

The final read-only evidence inspection at 2026-09-04 22:08 UTC covered the retained 250 signals, 20 feedback records, and 5,000 performance snapshots (787 distinct posts). Filtering excluded 15 unverified inferred-removal signals and 14 associated inferred negative-feedback records. There were 758 mature posts, 29 immature posts, and two complete approved edit pairs available within the retrieval budget. Current reward derivation changes cached action-reward calculations for 756 mature posts; that reflects recomputation against the current mature account baseline, not proof that all 756 prior observations were incorrect. No raw production records were changed during this inspection.

The runtime/evaluation follow-up passed 145 test files / 1,381 tests, TypeScript, and production build. Independent review verified deadline isolation, requested and provider-reported model identity, authenticated bounded transport, deployment pinning, and preview isolation. All 40 actual frozen packets passed envelope validation and gzip encode/decode with identical hashes and no model calls; the largest packet was 5,579,873 bytes before compression and 710,557 bytes compressed. This transport verification does not establish Astra writing quality.

PR #120 merged as `7966ae3a45da2ad7e98ecb1f675994799e332942`; exact deployment `dpl_7gNiD6t6257BPppuXHTiS8YDfc3L` became Ready at 2026-09-04 22:30:54 UTC. Both production aliases and health commits were verified, and merged-main CI passed. The first frozen comparison stopped after its control arm because the evaluator did not recognize the documented `gpt-5.6` to `gpt-5.6-sol` alias. All five calls returned Sol successfully; no fallback occurred. That attempt selected no drafts under the existing company-subject gate, spent an estimated $0.14255 using the old cost table, and did not execute the paired Astra arm. Its raw receipt is retained. The alias correction permits only the documented Sol mapping and same-model dated snapshots, retaining rejection of other model families, and updates the baseline cost table before a new comparison.

An isolated Astra run on the exact same frozen brief and deployment again hit the 75-second ideation deadline at high reasoning, with no provider usage or output receipt. Timeout cost is unknown, not zero. This does not establish whether latency, availability, or another provider-side condition caused the missing response. The next bounded experiment gives Astra ideation 120 seconds, matching the existing task-level allowance, while every call still obeys the shared 240-second run budget; GPT control remains at 75 seconds. Smaller batches alone did not resolve the timeout. The pilot remains disabled pending successful model output and the complete comparison gate.

The documented-alias, pricing, and 120-second ideation follow-up passed 146 test files / 1,399 tests, TypeScript, and production build. Independent review verified that other model families are still rejected, Sol cannot be mislabeled as an independent critic, and the remaining run budget overrides the longer Astra stage allowance. Existing historical cost receipts are preserved; missing estimates use current rates and are not historical invoice reconstruction.

PR #121 merged as `118b3f1a700461aad5ab46371d13de1c6d0d774e`; exact deployment `dpl_CLr5HmceffN4uoABjMmuwD3q1u1E` became Ready at 2026-09-04 22:40:45 UTC. Both production aliases returned successful homepages and the exact health commit, merged-main CI passed, and the protected account audit retained the verified learning derivation and two eligible queued control drafts. The final frozen attempt used this exact deployment and the corrected alias/cost policy. Its control arm completed with verified Sol identity; its ideas were rejected by existing quality gates, so it selected no draft.

At 2026-09-04 22:44:20 UTC, the paired Astra call timed out after 120,005 ms at high reasoning. It returned no content, token usage, or provider-reported model identity. The run correctly stopped with one attempted packet, zero complete valid pairs, zero decisive preference comparisons, and no quality-promotion result. The known control estimate was $0.05768; Astra cost remains unknown. The complete private comparison and gate-status receipts are retained under `.gstack/astra-evaluation/2026-09-04-rebuilt/run-2026-09-04T22-41-53-073Z/`.

A separate diagnostic on the frozen synthetic classroom-feedback profile succeeded on the same `118b3f1` deployment: all eight calls reported `gpt-6-astra`, with no fallback, and one selected draft passed the final critic. The full trace took 135,688 ms, used 17,056 input and 8,059 output tokens, and had a complete estimated cost of $0.57351. High-reasoning ideation took 52,097 ms. This establishes working Astra access and an end-to-end eligible result within the four-minute budget. It is a diagnostic arm, not a completed paired comparison or a preference vote.

Exact prompt replay found that Geoffrey's ideation sent 27,149 system/prompt characters, versus 14,487 for the successful synthetic profile; the much larger frozen storage packet was not sent wholesale to the model. Geoffrey's prompt also contained conflicting older/current model coaching, a parsed anti-goal containing positive SOUL themes, an unrelated seed, and prior-premise coverage text that displaced semantic fingerprints. Prompt compaction is therefore a concrete next experiment; the different synthetic subject and workload mean its success alone does not prove that prompt size caused Geoffrey's timeout.

The compact Astra request measures 9,753 characters including the unchanged 704-character schema, versus 27,853 before compaction: a 65.0% reduction on the same frozen Geoffrey packet. It retains evidence qualification, current directive precedence, account voice, subject/entity boundaries, frontier policy, and three distinct ideas. Prior-draft prose removal is shared by both model stacks; only Astra receives the compact idea-development contracts. Downstream writers, judges, factual gates, reasoning settings, and the 120-second ideation/240-second run deadlines remain in place. Unrelated accounts no longer inherit Anti Fund portfolio context from requested or sourced company subjects. Live diagnostic results follow below.

Final independent review reproduced and fixed long-history and sole-oversized-directive truncation, skipped-reason feedback, cached autopilot selection, disjoint billing/profile update races, and large-ID decoding. Complete recent directives take precedence over the soft guidance-character budget; an indivisible latest instruction may exceed that soft limit, while model/run deadlines remain hard. The final local regression run passed 148 test files / 1,448 tests, TypeScript, and production build.

PR #122 merged as `06f7f307940955874e40ef5658971021a1a4defa` after passing CI; merged-main CI also passed. Exact deployment `dpl_82tcRTuy1iXpZbbbDozyXG64sqxB` became Ready at 2026-09-04 23:08:47 UTC. Both production aliases and health endpoints were verified against that SHA. The protected account audit retained the verified learning derivation, GPT-control active stack, and two eligible queued drafts without quarantine. The subsequent Geoffrey diagnostic uses the original frozen packet on this exact deployment; it does not publish or change the queue.

The compact-prompt Geoffrey diagnostic started at 2026-09-04 23:09:29 UTC and again timed out in high-reasoning Astra ideation after 120,010 ms. It produced no ideas, drafts, provider-reported model identity, token usage, or cost receipt. This rules out prompt-size reduction as a sufficient fix; it does not identify the provider-side cause. The immutable receipt is retained privately as `frozen-geoffrey-06f7f30-2026-09-04T23-09-29-755Z.json`. The full comparison remains blocked by an invalid required arm, so no preference threshold or pilot eligibility is claimed.

Three independent diagnostic arms on the same frozen snapshot and exact `06f7f30` deployment establish the scope more precisely. All used high creative reasoning and medium judging, with no fallback, retry, queue writes, or publishing:

| Frozen subject | Actual Astra calls | Total elapsed | Known estimated cost | Outcome |
| --- | ---: | ---: | ---: | --- |
| Geoffrey: founder ownership | 6 | 130.6 seconds | $0.67888 | Three ideas, one selected idea, three drafts, zero final eligible drafts. Copy failed native voice, novelty, quality margin, and generated-pattern/cringe checks. |
| Geoffrey: nuclear fission deployment | 2 | 74.2 seconds | $0.23227 | Three ideas, zero selected. Financing/risk-allocation premises failed author fit, specificity, consequence, or comparison-pattern checks. |
| Synthetic teacher: classroom feedback | 2 | 47.7 seconds | $0.14437 | Three ideas, zero selected. Generic-premise, generated-pattern, and share-potential checks rejected them. |

These receipts confirm current Astra responsiveness, including a complete Geoffrey generation run, but provide no blinded preference or publishing-quality improvement claim. The earlier successful synthetic draft on `118b3f1` remains in the record; different code and stochastic samples are not interchangeable paired evidence. Known diagnostic costs exclude timed-out calls without usage receipts. The next bottleneck is completing Geoffrey's AI ideation within the bounded workflow and producing enough eligible drafts to support the required comparison.

The independent-review helper was also checked before any paid voting: it verifies the actual returned Sonnet 4.6 identity, stores immutable model responses before vote parsing, records input/prompt/comparison hashes and usage, and refuses duplicate batches. Six mocked failure/success scenarios passed. It has not produced live votes because no complete valid comparison exists.

**Release disposition:** shared correctness, learning, creative-context improvements, Astra integration, and evaluation tooling are live. `ASTRA_CREATIVE_ROLLOUT` remains disabled; Geoffrey continues on the GPT control stack. Astra execution is verified on Geoffrey and synthetic profiles, but the required AI-inference arm still times out and the additional diagnostics produced no eligible final drafts. All 40 briefs are frozen and validated; the paired comparison is incomplete, with zero decisive preference comparisons and no promotion result. The Astra pilot has not started, so there are no mature pilot posts and the seven-day/ten-post expansion clock has not begun. The original posting cadence, factual/voice standards, and raw learning history remain preserved. No writing-quality improvement or future virality is inferred from these receipts.

## Completion and creative-execution repair

The follow-up investigation reproduced a hidden transport behavior with the installed OpenAI SDK and a local fake HTTP provider: one application attempt received a 503 and retried internally before returning 200. Astra now disables SDK retries, letting the explicit model chain record each failure. Astra Responses streaming records the response ID, wire model, status, first event, first output, and last event. Only a full terminal response can supply copy; deltas, premature EOF, status mismatch, abort, and error events cannot become accepted text. This instrumentation distinguishes provider acceptance from completed generation; it does not retroactively establish the cause of older timeouts.

Astra ideation now assigns three separate substantive approaches to independent one-proposition requests: a conviction, a decision/question, and an institutional consequence. Requests rotate across briefs before taking another approach, with at most four concurrent calls and three propositions per brief. The entire Astra idea stage, including retries, shares 120 seconds and respects the remaining 240-second run deadline. Completed propositions survive stage expiry; queued work stops so writers and judges retain time. Exact brief binding, combined normalization, existing semantic deduplication, and proposition caps prevent extra model output from gaining extra selection chances. Creative reasoning remains high. This scheduling change is a hypothesis about completion reliability until the new live receipts establish its effect.

The founder-ownership receipt identified a separate reproducible creative defect: the selected idea's distinctive consequence was an override on everyone still building, but a writer instruction explicitly told the direct-judgment variant to stop before the consequence. All single-call variants were also forced toward the shortest shape. Writers now retain the approved idea's specific stakes and use a natural shape appropriate to its voice anchors. Parsed positive SOUL sections are separated from prohibitions without changing the SOUL. Ideation excludes raw example/prior-post sections even when nested inside malformed anti-goals.

The same founder receipt contained useful critic feedback that could not enter the old near-miss repair path. Astra now permits one parent-linked, critic-directed execution repair for a strong approved idea and a fact-safe judged draft whose failures concern voice, expression, novelty, or margin. It requires a substantive critic diagnosis and sufficient remaining time. The repair receives the original critic notes and approved evidence, then passes the same deterministic checks and final critic. Factual failures, copied premises, unavailable judgment, already-mutated drafts, and exhausted deadlines do not qualify. Admission to a rewrite does not change publishing thresholds. Tests replay the recorded founder failure and verify that a still-failing revision remains rejected.

Evaluation previously stopped scheduling after the first invalid arm and counted factual failures only on drafts, although unsupported facts can be rejected at the idea stage. Explicit complete-suite execution now separates attempted completion from promotion-valid completion. It retains failures and cohort-level results, counts idea and draft factual failures separately with their denominators, and reports one-sided eligible-copy versus valid-empty utility separately from decisive copy preference. Authentication, integrity, unbounded unknown spend, interruption, and budget limits still stop new work. The original 40-packet snapshot remains unchanged; no diagnostic is substituted into a paired vote.

An Astra request records a conservative unknown-usage budget reservation before dispatch: serialized request UTF-8 bytes plus a 16,384-token framing allowance, the actual output-token limit, and current pricing including long-context multipliers. Evaluation recomputes that reservation, counts each provider attempt once, and stops scheduling against known estimates plus unknown-attempt reservations. A reservation is neither an observed charge nor a contractual billing maximum. Missing actual usage remains unknown and cannot become promotion-valid evidence. Unknown attempts without an auditable reservation still stop scheduling; active arms drain into receipts.

A read-only source audit on 2026-09-04 inspected 400 cached documents (950 claim atoms) and 159 stories. Only one story cleared the current planning/account gates, the NASA/Blue Origin Mars telecom case already in the frozen snapshot. Thus the original benchmark contains 29 source-free Geoffrey topics, one qualified source case, and ten synthetic source-free profiles. It is a declared subject stress set constrained by current source supply, not an empirical sample of production traffic. A coverage manifest makes this limitation explicit; replacing weak cases after observing model outputs would not establish a fair comparison.

The completion/creative-execution follow-up passed 149 test files / 1,498 tests, TypeScript, and production build locally. Independent reviews covered the shared stage budget, combined retry normalization, exact brief binding, failed-stream provenance, repair admission and re-rejection, and unknown-cost accounting. Live results and exact deployment verification follow after release; local tests do not establish a preference improvement or pilot eligibility.
